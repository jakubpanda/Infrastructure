jenkins:
  securityRealm:
    local:
      users:
        - id: "panda"
          name: "panda"
          password: "panda"
  authorizationStrategy:
    globalMatrix:
      permissions:
      - "USER:Overall/Administer:panda"
  numExecutors: 0
unclassified:
  locale:
    systemLocale: "en_EN"
    ignoreAcceptLanguage: true


      